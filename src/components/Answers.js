import { Typography } from "@mui/material";
import React from "react";

const Answers = () => {
  return (
    <div>
      <Typography>Answers page</Typography>
    </div>
  );
};

export default Answers;
